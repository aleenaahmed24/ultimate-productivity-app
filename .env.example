# Google Cloud OAuth 2.0 Client ID (Web application type).
# Required only if you want the "Connect Google Calendar" feature to work.
# This is a public client ID, safe to expose in the built frontend —
# never put a client *secret* here.
VITE_GOOGLE_CLIENT_ID=
